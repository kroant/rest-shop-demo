application:
  scheduling:
    enable: false

spring:
  web:
    locale: en
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    open-in-view: false
    show-sql: true
    hibernate.ddl-auto: none
    properties:
      hibernate:
        format_sql: true

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false